# User Issue Ledger: Operations / scheduler priority

| ID | Applies to | Mistake pattern | Required behavior | Prevention and verification |
| --- | --- | --- | --- | --- |
| UIL-OPERATIONS-SCHEDULER-PRIORITY-001 | Physical admission across FAST lanes | Archive-specific recovery was elevated into the physical scheduler and competed with ordinary campaign work, expanding a legacy-data concern into a permanent scheduling state machine. | Physical FAST admission has no historical archive or audit lane. Recorded whole-polar promotion retains its normal first turn; otherwise ordinary campaign PRECALC work runs immediately, followed by the standard FAST/FULL ladder under the shared capacity and disk guards. | A scheduler-order regression proves promotion short-circuits campaign work and an empty promotion path calls campaign PRECALC directly, with no archive callback available. Production logs must contain no archive-recovery routing while current-result archive publication and real FAST/FULL calculation continue. |
| UIL-OPERATIONS-SCHEDULER-PRIORITY-002 | Weighted local and remote CPU admission | Fixed case concurrency made an otherwise runnable batch an indivisible 8–17-slot reservation. Smaller running jobs left a persistent packing hole (for example 4+8/8 or 62+17/64), so real CPUs idled despite a large eligible backlog. | At the serialized admission boundary, shrink only execution-policy case concurrency to the largest positive weight that fits the exact remaining node cap. Update the durable reservation and stored resource payload before the engine call. Never change physics/numerics, exceed the cap, reduce solver processes, or admit when even one configured solver-process group cannot fit. | Regressions require a 4+8/8 candidate to submit at weight 4 and a 62+17/64 candidate to submit at weight 2, while zero remainder and solver-processes-greater-than-remainder remain denied. Production verification compares durable weights, actual OpenFOAM children, and the unchanged configured cap. |
| UIL-OPERATIONS-SCHEDULER-PRIORITY-003 | Capacity-denied composition cleanup and tick progress | Repeated over-capacity compositions were cancelled, but a JavaScript `Date` reached a string encoder later in the tick. The independent heartbeat stayed green while every tick failed, making the scheduler appear ongoing although `lastTickCompletedAt` stopped advancing. | Capacity denial and any adaptive-resize settlement use database `now()` expressions, not driver-bound JavaScript dates, and the tick must either complete or log the exact named failing stage. A denied composition releases its claims without turning a live sweeper heartbeat into false progress. | An attached-result capacity regression exercises the complete denial cleanup repeatedly and requires no type error plus advancing tick completion. Production verification requires fresh heartbeat and fresh completed-tick timestamps on both nodes with no `ERR_INVALID_ARG_TYPE` recurrence. |
