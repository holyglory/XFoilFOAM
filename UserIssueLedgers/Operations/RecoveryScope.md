# User Issue Ledger: Operations / recovery scope

| ID | Applies to | Mistake pattern | Required behavior | Prevention and verification |
| --- | --- | --- | --- | --- |
| UIL-OPERATIONS-RECOVERY-SCOPE-001 | Legacy solver evidence and production-quality recovery | Inherited historical-audit machinery was treated as an operational requirement, and work expanded into archive reassessment and increasingly complex result-specific recovery even though the user wanted ordinary fresh recalculation, full solver utilization, and maintainable operations. | Historical archives are never audited or restored automatically. Delete every old aerodynamic result generation—including previously accepted canonical generations—through the flat solver-domain reset, return current points to the ordinary unsolved queue, and keep only non-solver catalog/setup/campaign truth plus operational canaries. A legacy defect never justifies a fleet-wide fence, background audit campaign, retroactive reinterpretation, or special publication path. | Before implementation or deployment, distinguish requested fresh computation from optional historical reassessment and ledger only the former as scope. Regressions must prove legacy provenance defects cannot start an audit or stop unrelated capacity, and production verification must use a fresh current-engine result through the normal ingest/archive/publication path. Any future audit execution requires a separate explicit user request and a new decision superseding D-2026-08-09. |
