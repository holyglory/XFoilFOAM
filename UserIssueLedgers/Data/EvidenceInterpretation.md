# User Issue Ledger: Data / evidence interpretation

| ID | Applies to | Mistake pattern | Required behavior | Prevention and verification |
| --- | --- | --- | --- | --- |
| UIL-DATA-EVIDENCE-INTERPRETATION-001 | Integer-period URANS acceptance boundaries and solver incidents | A physically exact integer-period window can serialize a few floating-point ULPs below its configured minimum (for example, `2.9999999999999996` for a three-period FAST window). A literal `>=` check then misclassifies complete evidence as `insufficient-periods`, and repeated false rejections can trigger a systemic safety stop. | Treat only finite values within a narrowly derived floating-point boundary tolerance of the exact configured integer minimum as meeting that minimum. Values materially below the boundary, non-finite values, non-stationary evidence, and every other publication gate remain rejected. Reconcile affected current generations only through authenticated archive re-reduction/reclassification; never rewrite raw attempts or invent coefficients. | Core regressions must accept exact and ULP-scale-underflow representations at FAST and FINAL boundaries while rejecting a value just outside the tolerance. Producer/reducer tests must pin the same boundary semantics. Migration coverage must prove recovery is limited to current PRECALC evidence whose sole rejection is the roundoff-only period reason, preserves immutable attempts/results, and queues exact archive re-reduction rather than directly publishing coefficients. |
